# THOUGHT Protocol

`CURRENT.json` identifies the active protocol release. Immutable V1 history lives under `history/v1`; normative V2 artifacts live under `releases/v2`; transport integration is separate under `integrations`.

Do not treat older monolithic implementation briefs or renderer bridge artifacts as competing protocol authorities.
