# Historical spec bytes

`THOUGHT.v2.md` in this directory belongs to the unpublished binary-weave
attempt. Its exact bytes are retained for reproducibility and must not be
registered or deployed as the current THOUGHT V2 specification.

The current V2 Terminal English specification remains a release gate and will
replace this historical root-level artifact only after review.
